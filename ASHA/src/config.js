export const API_URL = import.meta.env.VITE_API_URL || "http://localhost:4000/api";
// Define la constante API_URL que representa la URL de la API
// Utiliza import.meta.env para acceder a las variables de entorno proporcionadas por Vite
// VITE_API_URL es una variable de entorno definida en el entorno de Vite
// Si VITE_API_URL está definido, utiliza su valor como la URL de la API
// De lo contrario, utiliza "http://localhost:4000/api" como valor predeterminado
